# 仿Everything文件检索系统｜UI设计方案（V2.0 — Everything原生体验）

> **设计理念**：极致简约 — 一个搜索框 + 一张结果列表 = 全部交互
> **核心差异（vs V1.0）**：移除「搜索按钮」→ 改为**边输入边检索**；移除底部详情区/状态栏→ **结果区一体化**；新增**系统托盘 + 全局热键 + 键盘完全驱动**

## 一、Everything 原生界面还原度分析

| Everything 特征 | 还原方案 | 实现方式 |
|----------------|---------|---------|
| 极简窗口：仅搜索栏+结果列表 | 2 区域布局（替代原 5 分区） | 搜索栏置顶 + Treeview 占满剩余空间 |
| 即时搜索：输入即出结果 | 输入防抖(300ms)自动触发 | `<KeyRelease>` 绑定 + threading.Timer |
| 无搜索按钮 | 删除「开始检索」按钮 | 关键词变化自动触发 |
| 键盘驱动操作 | 全键盘可完成所有操作 | 方向键导航 / Enter 打开 / Esc 清空 |
| 系统托盘常驻 | 最小化到托盘 | `pystray` 或 Win32 API |
| 全局热键唤醒 | Ctrl+Ctrl 弹出窗口 | `keyboard` 库或注册全局热键 |
| 超轻量窗口 | 固定小尺寸 600×480 | 可拖拽调整大小 |

---

## 二、整体 UI 布局架构（2 分区极简布局）

### 2.1 布局总览

```
窗口尺寸: 600×480 (默认), 可自由缩放, 记住用户上次尺寸
窗体风格: 无标题栏工具窗口(ToolWindow) 或 轻量标题栏

┌──────────────────────────────────────────────────┐
│  🔍 │ 搜索框 (输入即搜)              │ ⚙️  🗘    │ ← 搜索栏区
├──────────────────────────────────────────────────┤
│  文件名        │ 路径           │ 大小   │ 日期  │ ← 表头
│──────────────────────────────────────────────────│
│  report.pdf    D:\Docs\Work     2.1 MB  06-03   │
│  report_v2.pdf D:\Docs\Work     1.8 MB  05-28   │
│  summary.docx  D:\Docs\Work     156 KB  06-01   │
│  data_report.xl.. C:\Data       3.4 MB  04-15   │
│  ...                                              │ ← 结果列表(占90%+空间)
│                                                    │
│                                                    │
├──────────────────────────────────────────────────┤
│  已找到 156 个文件 (0.32s)                        │ ← 迷你状态行
└──────────────────────────────────────────────────┘
```

### 2.2 与 V1.0 的布局对比

| 维度 | V1.0（旧版） | V2.0（Everything 风格） |
|------|-------------|----------------------|
| **区域数** | 5 个独立分区 | **2 个区域**（搜索栏 + 结果列表） |
| **搜索触发** | 点击「开始检索」按钮 | **输入即搜（300ms 防抖）** |
| **筛选栏** | 独立工具栏区域 | **收纳到搜索语法或右键菜单** |
| **详情面板** | 底部 90px 详情预览区 | **删除（选中项信息在状态行显示）** |
| **状态栏** | 独立 30px 状态栏 | **合并为结果列表底部的迷你状态行** |
| **目录选择** | 顶部目录输入框+浏览按钮 | **顶部路径栏 + 下拉历史** |
| **窗口尺寸** | 1100×720 固定大窗 | **600×480 默认小窗，可缩放** |

---

## 三、分区域详细设计

### 3.1 搜索栏区（区域高 ~45px，唯一的核心交互区）

这是整个应用的**灵魂区域**，对标 Everything 顶部的搜索输入框。

#### 布局结构（从左到右）

| # | 控件 | 类型 | 功能说明 |
|---|------|------|---------|
| ① | 图标 | Label | 搜索图标（放大镜），视觉引导 |
| ② | 搜索输入框 | Entry（宽占满剩余空间） | **核心控件**：输入关键词/通配符/搜索语法 |
| ③ | 设置按钮 | Button（小图标⚙️） | 弹出设置菜单（排序/筛选/导出等） |
| ④ | 最小化按钮 | Button（🗘） | 最小化到系统托盘（非关闭） |

#### 搜索输入框详细设计

```python
search_entry = Entry(
    width=60,
    font=("Segoe UI", 10),
    placeholder_text="搜索文件... (支持 *.pdf, abc:, !mp3 等语法)",
)
```

**交互行为（Everything 核心体验）：**

| 用户行为 | 系统响应 |
|---------|---------|
| 输入/修改文字 | 启动 **300ms 防抖计时器**，超时后自动触发检索 |
| 按 Enter | **立即执行检索**（跳过防抖等待） |
| 按 Escape | **清空搜索框 + 清空结果列表** |
| 按 ↓ (下箭头) | **焦点从搜索框跳转到结果列表第一行** |
| 输入为空且停留 500ms | 自动显示最近检索历史（下拉建议列表） |
| 文本变化时取消上一次未完成的检索 | **中断标记位(stop_flag)** 取消前一次遍历线程 |

#### 搜索语法支持（对标 Everything）

| 语法示例 | 含义 | 实现方式 |
|---------|------|---------|
| `report` | 文件名包含 "report"（模糊匹配） | 小写包含匹配 |
| `*.pdf` | 所有 PDF 文件 | pathlib rglob 通配 |
| `report.pdf` | 文件名含 report 且扩展名为 pdf | 多条件 AND 匹配 |
| `!mp3` | 排除 mp3 文件 | 排除过滤 |
| `abc:` | 在指定路径/目录中搜索 | 路径限定搜索 |
| `dm:>1mb` | 大于 1MB 的文件 | 大小过滤 |
| `rc:today` | 今天修改的文件 | 时间范围过滤 |

> 注：高级语法（`!排除`、`大小过滤`、`时间过滤`）属于 P1/P2 拓展功能，基础版先实现模糊匹配和通配符。

### 3.2 结果列表区（占据剩余全部空间）

采用 **Treeview 表格控件**，对标 Everything 的结果展示。

#### 默认列配置（5 列）

| 列名 | 宽度 | 对齐 | 内容来源 |
|------|------|------|---------|
| 文件名 | *（自适应填充） | 左对齐 | `file.name` |
| 路径 | 200px | 左对齐 | `file.parent` 相对路径 |
| 大小 | 70px | 右对齐 | `format_size(st_size)` |
| 修改日期 | 80px | 左对齐 | `format_time(st_mtime)` |
| 类型 | 60px | 左对齐 | `get_file_type(suffix)` |

#### 交互行为（键盘 + 鼠标双驱动）

**鼠标操作：**

| 操作 | 行为 |
|------|------|
| 单击某行 | 高亮选中该行，底部状态行更新为该文件信息 |
| 双击某行 | **直接打开文件**（用系统默认程序） |
| 右键某行 | 弹出上下文菜单（见下方菜单定义） |
| 单击表头 | **按该列排序**（切换升序/降序） |
| 滚轮滚动 | 正常滚动浏览结果 |

**键盘操作（Everything 风格核心）：**

| 快捷键 | 行为 | 说明 |
|--------|------|------|
| ↑ / ↓ | 上/下移动选中行 | 循环滚动（到底后回到顶部） |
| Enter | **打开选中的文件** | 等同于双击 |
| Ctrl + Enter | **打开文件所在文件夹** | 资源管理器定位 |
| Ctrl + C | **复制文件完整路径**到剪贴板 | |
| Delete | 从结果列表中移除该项（不删文件） | 过滤掉不关心的结果 |
| F2 | 重命名文件 | 调用系统重命名 |
| Ctrl + A | 全选所有结果 | 用于批量导出 |
| Tab | 焦点在搜索框和结果列表间切换 | |
| Escape | 清空搜索 + 清空结果 | 回到初始状态 |
| Ctrl + E | 导出当前结果 | 弹出保存对话框 |

#### 右键上下文菜单

```
┌─────────────────────┐
│ 📂 打开文件          │  Enter
│ 📁 打开所在位置      │  Ctrl+Enter
│ ─────────────────── │
│ 📋 复制名称          │
│ 📋 复制路径          │  Ctrl+C
│ 📋 复制完整路径      │
│ ─────────────────── │
│ ✏️  重命名            │  F2
│ 🗑️  删除文件         │  （需二次确认）
│ ─────────────────── │
│ 📤 导出选中项...      │  Ctrl+E
│ 📤 导出全部结果...    │
│ ─────────────────── │
│ ℹ️  属性              │  显示文件详细信息弹窗
└─────────────────────┘
```

#### 空状态设计

当结果列表为空时：

- 搜索框为空 → 显示灰色提示文字：「输入关键词开始搜索文件」
- 有搜索词但无结果 → 居中显示：
  ```
  😕 未找到匹配的文件
  
  建议:
  · 检查拼写是否正确
  · 尝试使用通配符 (*.pdf)
  · 更换检索目录
  ```

### 3.3 迷你状态行（紧贴结果列表底部，高 ~24px）

取代原来的独立状态栏和底部详情区。

```
┌──────────────────────────────────────────────────────────┐
│  已找到 156 个文件  │  用时 0.32s  │  D:\Documents       │
└──────────────────────────────────────────────────────────┘
```

| 信息块 | 内容 | 动态更新时机 |
|--------|------|------------|
| 左侧 | `已找到 N 个文件` | 每次检索完成后 |
| 中侧 | `用时 X.XXs` | 检索完成后 |
| 右侧 | 当前检索目录路径 | 切换目录时更新 |

**选中某行时的状态行变化：**
```
已找到 156 个文件 │ report.pdf │ 2.1 MB │ 2026-06-03 14:20 │ D:\Docs\Work
```

---

## 四、系统托盘与全局热键（Everything 标志性功能）

### 4.1 系统托盘集成

| 行为 | 效果 |
|------|------|
| 点击托盘图标 | 显示/隐藏主窗口（toggle） |
| 右键托盘图标 | 弹出快捷菜单：显示 / 退出 |
| 关闭窗口按钮(X) | **最小化到托盘**（不退出程序） |

### 4.2 全局热键

| 热键 | 行为 | 说明 |
|------|------|------|
| **Ctrl + Ctrl**（连按两次 Ctrl） | 唤醒/隐藏窗口 | 对标 Everything 默认热键 |
| **Ctrl + Alt + F** | 唤醒窗口并聚焦搜索框 | 备选热键 |

> 技术实现：Windows 下使用 `win32api.RegisterHotKey` 注册全局热键，或使用 `keyboard` 第三方库。若不允许第三方库则仅实现托盘功能。

### 4.3 窗口管理策略

| 操作 | 行为 | 对标 Everything |
|------|------|---------------|
| 启动程序 | 直接显示窗口 | 一致 |
| 点击最小化 | 最小化到任务栏 | 一致 |
| 点击关闭(X) | **最小化到系统托盘** | 一致（Everything 默认行为） |
| 托盘右键→退出 | 彻底退出程序 | 一致 |

---

## 五、目录选择机制（重新设计）

Everything 不需要每次手动选目录——它默认全盘索引。本项目因技术约束需要指定目录，因此优化目录选择体验：

### 5.1 顶部路径栏（搜索栏上方或内嵌）

方案 A（推荐）：搜索栏上方增加一行紧凑路径栏

```
┌──────────────────────────────────────────────────┐
│ [D:\Documents ▼]  [📁]  [🔄]                    │ ← 路径栏
├──────────────────────────────────────────────────┤
│ 🔍 [搜索文件...]                           ⚙️ 🗘  │ ← 搜索栏
└──────────────────────────────────────────────────┘
```

| 控件 | 功能 |
|------|------|
| 目录下拉框 | 显示历史使用的目录，点击快速切换 |
| 📁 浏览按钮 | 弹出文件夹选择对话框 |
| 🔄 刷新按钮 | 使用相同关键词重新扫描目录（目录内容可能已变） |

方案 B（更极简）：将目录选择收纳到设置菜单(⚙️)中，搜索栏仅做搜索

### 5.2 目录切换流程

```mermaid
flowchart LR
    A[用户点击目录下拉] --> B{选择目录}
    B --> C[更新当前检索目录]
    C --> D{搜索框有内容?}
    D -->|有| E[自动用新目录+原关键词重新检索]
    D -->|无| F[仅更新目录,等待用户输入]
```

---

## 六、设置面板（点击⚙️弹出）

以 **模态对话框 / 下拉面板** 形式出现，收纳所有非核心功能：

| 分类 | 设置项 | 默认值 |
|------|--------|-------|
| **排序** | 按名称/大小/日期/路径 × 升序/降序 | 名称升序 |
| **筛选** | 文件类型 / 大小范围 / 时间范围 | 全部不限 |
| **显示** | 显示哪些列（勾选开关） | 全部显示 |
| **通用** | 防抖延迟(ms) / 是否开机自启 / 深色模式 | 300ms / 关 / 浅色 |
| **高级** | 最大结果显示数 / 是否隐藏隐藏文件 | 10000 / 是 |

---

## 七、配色规范（Everything 原生风格）

### 7.1 浅色主题（默认，对标 Everything Default Theme）

| 元素 | 色值 | 说明 |
|------|------|------|
| 窗体背景 | `#FFFFFF` | 纯白背景（Everything 就是纯白） |
| 搜索框背景 | `#FFFFFF` | 白色输入框 |
| 搜索框聚焦边框 | `#0078D4` | Windows 蓝色聚焦环 |
| 结果列表背景 | `#FFFFFF` | 白色 |
| 选中行高亮 | `#0078D4` 半透明蓝 | `rgba(0,120,212,0.15)` |
| 选中行文字 | `#000000` | 黑色 |
| 普通行文字 | `#1E1E1E` | 近黑（VS Code 同款文字色） |
| 表头背景 | `#F0F0F0` | 极浅灰 |
| 表头文字 | `#3C3C3C` | 深灰 |
| 状态行背景 | `#F5F5F5` | 浅灰底 |
| 状态行文字 | `#6E6E6E` | 中灰 |
| 分隔线 | `#E5E5E5` | 浅分隔线 |

### 7.2 深色主题（可选拓展）

| 元素 | 色值 | 说明 |
|------|------|------|
| 窗体背景 | `#1E1E1E` | VS Code 暗色底 |
| 搜索框背景 | `#2D2D30` | 稍亮输入区 |
| 选中行高亮 | `#094771` | 深蓝高亮 |
| 普通行文字 | `#D4D4D4` | 浅灰文字 |
| 表头背景 | `#2D2D30` | 暗灰表头 |
| 状态行背景 | `#252526` | 暗底状态栏 |

### 7.3 配色对比（V1.0 vs V2.0）

| 元素 | V1.0 旧配色 | V2.0 Everything 配色 | 差异原因 |
|------|-----------|--------------------|---------|
| 主背景 | `#f5f6f7` 浅灰白 | `#FFFFFF` 纯白 | Everything 是纯白底 |
| 主色调 | `#2563eb` 蓝色按钮 | `#0078D4` Windows 系统蓝 | 更贴近 Windows 原生感 |
| 选中行 | 未明确 | 半透明蓝底 + 黑字 | Everything 选中效果 |
| 按钮 | 圆角蓝色实体按钮 | 扁平化图标按钮 | Everything 无圆角按钮 |

---

## 八、完整交互逻辑流（V2.0 版本）

### 8.1 即时搜索核心流程

```
用户在搜索框输入字符
    │
    ├─→ 触发 <KeyRelease> 事件
    │
    ├─→ 取消上一次防抖 Timer（如有）
    │
    ├─→ 设置 stop_flag → 中断正在执行的上一轮检索线程
    │
    ├─→ 启动新的 300ms 防抖 Timer
    │       │
    │       └─→ 300ms 内无新输入?
    │           │
    │           ├─ 否 → 重复上述流程（被下一次 KeyRelease 取消）
    │           │
    │           └─ 是 → 【执行检索】
    │                   │
    │                   ├─→ 校验: 目录是否已选? 关键词是否非空?
    │                   │     └─ 未选目录 → 提示先选目录
    │                   │
    │                   ├─→ 创建新 Thread → search_engine.scan_files()
    │                   │     ├─→ rglob("*") 遍历
    │                   │     ├─→ 关键词匹配
    │                   │     └─→ 定期检查 stop_flag，被设则中止
    │                   │
    │                   ├─→ 线程结束回调 → data_utils 处理
    │                   │
    │                   └─→ after() 回主线程 → 刷新 Treeview
    │                         └─→ 更新状态行统计
    │
    └─→ 用户按 Enter → 跳过防抖，立即执行上述【执行检索】
```

### 8.2 线程生命周期管理

```
检索线程生命周期:

创建 Thread → 执行 scan_files()
    │
    ├── 正常完成 → 回调刷新 UI → 线程结束
    │
    ├── 被 stop_flag 中断 → 线程安全退出 → 新线程接替
    │
    └── 异常退出 → 记录日志 → 状态行显示错误提示
```

**关键规则：同一时刻最多只有 1 个活跃检索线程。** 新检索发起时必须先中断旧的。

---

## 九、窗口启动与关闭行为

### 9.1 启动序列

```
程序启动
    │
    ├─→ 读取配置文件（窗口位置/大小/上次目录/主题）
    │
    ├─→ 创建主窗口（恢复上次位置和尺寸）
    │
    ├─→ 初始化系统托盘图标
    │
    ├─→ 注册全局热键（Ctrl+Ctrl）
    │
    ├─→ 加载检索历史到目录下拉框
    │
    └─→ 聚焦搜索框，光标闪烁等待输入
```

### 9.2 关闭序列

```
用户点击关闭按钮(X)
    │
    ├─→ 不是"退出"操作 → 最小化到系统托盘
    │     └─→ 保存当前窗口位置/尺寸到配置
    │
    └─→ 用户从托盘选择"退出"
          ├─→ 中断正在执行的检索线程
          ├─→ 保存配置和历史记录
          ├─→ 注销全局热键
          └─→ 销毁窗口，进程退出
```

---

## 十、落地开发拆分（V2.0 新版）

| 步骤 | 任务 | 工作量 | 核心要点 |
|------|------|--------|---------|
| **Step 1** | 搭建 2 区极简窗口骨架（搜索栏+结果列表+状态行） | 0.5 天 | 移除多余区域，纯白主题 |
| **Step 2** | 实现**即时搜索**（防抖+子线程+中断机制） | 1 天 | 核心：输入即搜，无按钮 |
| **Step 3** | 实现键盘完全驱动（方向键/Enter/Esc/Ctrl+C 等） | 0.5 天 | 所有操作无需鼠标 |
| **Step 4** | 实现右键菜单 + 双击打开 + 表头排序 | 0.5 天 | 交互完善 |
| **Step 5** | 实现系统托盘 + 全局热键 | 0.5 天 | Everything 标志性功能 |
| **Step 6** | 实现设置面板（筛选/排序/显示列/主题切换） | 1 天 | P1 拓展功能 |
| **Step 7** | 实现搜索语法解析（通配符/排除/大小/时间） | 0.5 天 | P2 高级功能 |
| **总计** | | **4.5 天** | |

---

## 十一、对接作业文档说明

> `说明.txt` 可填写的 UI 相关亮点：
>
> ### 核心亮点（P0）
> 1. **即时搜索体验**：输入即搜，无需点击搜索按钮，300ms 智能防抖
> 2. **Everything 极简界面**：仅搜索框+结果列表两区域，零冗余 UI
> 3. **键盘完全驱动**：方向键导航、Enter 打开、Esc 清空，鼠标非必需
>
> ### 拓展功能（P1/P2）
> 4. **系统托盘常驻 + 全局热键唤醒**（Ctrl+Ctrl）
> 5. **智能搜索语法**：支持通配符 `*.pdf`、排除 `!mp3`、大小过滤 `dm:>1mb`
> 6. **实时中断与替换**：输入变化时自动取消上一次未完成的检索
> 7. **深色模式 / 自定义列显示 / 结果导出**
